import React, { FC } from 'react';

interface BaseAlertProps {
  className?: string;
}

const Template: FC<BaseAlertProps> = () => {
  return <div></div>;
};

Template.defaultProps = {};

export default Template;
